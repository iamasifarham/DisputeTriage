The login OTP is captured in MailTrap. i have not shared my MailTrap sandbox credentials here, 
So I just added a line inside #" auth.py "# line 54, 55, 56 for printing the OTP in the Terminal for test purposes and can be removed with just removing the 3 lines. 

#_Before running the system please make sure you enable python virtual Environment (venv) from the root directory and install the python dependencies "requirements.py" from the root directory. /root> pip install requirements.py

And also install the node modules dependencies from /.../frontend> npm install

The "backend" and "frontend" folders should be inside the "root" folder.

Inside the /dispute_backend> Dispute Triage System.pptx is provided please find this attached.


-------------LOGIN Credentials-------------------

Admin (initializes):

username: admin
password: admin123

     ---------------------

Employee:

username: thedarkknight
password: password